{
  "name": "stredis",
  "version": "0.1.0",
  "type": "module",
  "exports": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "sideEffects": false,
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "prepublishOnly": "yarn run test && yarn run build",
    "test": "yarn run check && yarn run lint",
    "check": "yarn run compile --noEmit",
    "lint": "xo",
    "clean": "rimraf lib",
    "prebuild": "yarn run clean",
    "build": "yarn run compile",
    "compile": "tsc",
    "watch": "yarn run compile --watch",
    "predev": "yarn run clean",
    "dev": "yarn run watch  --sourceMap"
  },
  "repository": "git@github.com:lolPants/stredis.git",
  "author": "Jack Baron <jackmbaron@gmail.com>",
  "license": "MIT",
  "private": false,
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "@types/chunk": "^0.0.0",
    "@types/ioredis": "^4.26.1",
    "@types/node": "^15.0.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4",
    "xo": "^0.40.0"
  },
  "dependencies": {
    "chunk": "^0.0.3",
    "ioredis": "^4.27.2"
  }
}
